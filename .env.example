# kuake / SDK 相关环境变量示例
#
# 用法：
#   1. 复制本文件为 `.env`，按需取消注释并填入真实值；
#   2. 将 `.env` 放在当前工作目录；`kuake` 解析完参数后会自动加载（不覆盖已在 shell/CI 里 export 的变量）；
#   3. 关闭自动加载：设置 `KUAKE_LOAD_DOTENV=0`（仅此一种取值表示关闭）。
#
# 凭证优先级（详见 docs/cli.md）：
#   KUAKE_COOKIE（整段）> KUAKE_PUS + KUAKE_PUUS（拆分）> -cookies/--cookies
#
# v1.5.0 BREAKING：已移除 config.json 支持，凭证只从上述三种来源读取。
#
# 勿将含真实 Cookie 的 `.env` 提交到版本库（仓库 `.gitignore` 已忽略 `.env`）。

# ---------------------------------------------------------------------------
# CLI（cmd/kuake）与会话
# ---------------------------------------------------------------------------

# 设为 `0` 时，不自动从 `.env` 注入环境变量。
#KUAKE_LOAD_DOTENV=0

# --- 方式 A：整段 Cookie（优先；非空则忽略下方 KUAKE_PUS / KUAKE_PUUS）---
#KUAKE_COOKIE='__pus=...; __puus=...; ...'

# --- 方式 B：仅拆 __pus / __puus（仅当方式 A 未设置或 trim 后为空时生效）---
# 值为裸串，不要写 __pus= / __puus= 前缀；可只填一侧。
#KUAKE_PUS=你的__pus值不含前缀
#KUAKE_PUUS=你的__puus值不含前缀

# 说明：`-cookies` / `--cookies` 仅为命令行参数，没有同名环境变量。

# 上传并行度（1–16）。未传 `--max_upload_parallel` 时读取；传入 flag 时 flag 优先。
#KUAKE_UPLOAD_PARALLEL=4

# ---------------------------------------------------------------------------
# SDK 调试（变量名大小写以代码为准）
# ---------------------------------------------------------------------------

# 设为 1 开启 SDK 调试输出（如 DownloadFile 请求/响应摘要）。
#KUake_DEBUG=1

# ---------------------------------------------------------------------------
# 集成测试 / E2E（go test ./sdk，非日常 kuake 命令）
# ---------------------------------------------------------------------------
# E2E 仅使用与上方相同的 KUAKE_COOKIE / KUAKE_PUS+KUAKE_PUUS；测试会尝试加载 cwd 下的 .env。

# 任一为 1 时启用 E2E 回归（见 sdk/e2e_regression_test.go）。
#E2E_REGRESSION=1
#INTEGRATION_TEST=1

# ---------------------------------------------------------------------------
# kuake-mcp（stdio MCP server）专用变量
# ---------------------------------------------------------------------------
# 通常通过 .mcp.json 的 "env" 字段配置（见 .mcp.json.example），不写在这里。
# 列在此处仅作参考。

# 冒号分隔的禁用操作名。常用：upload / delete / move / copy / rename /
# create / share_create / share_delete / share_save / user。
#KUAKE_DENY_OPS=delete:move:share_create:share_save

# 冒号分隔的远端禁用路径前缀（命中前缀或路径相等的远端操作被拒）。
#KUAKE_DENY_PATHS=/重要资料:/备份

# 冒号分隔的禁止上传扩展名（按小写匹配）。
#KUAKE_DENY_EXTS=.env:.key:.pem

# 上传单文件大小上限（MiB），超过即拒；0 或不设表示不限制。
#KUAKE_MAX_UPLOAD_MB=200

# 下载沙箱根目录；quark_download 把文件写入此路径 + tool 参数 local_sub_dir。
# 不设默认 = 当前工作目录。
#KUAKE_DOWNLOAD_DIR=/tmp/quark-downloads
